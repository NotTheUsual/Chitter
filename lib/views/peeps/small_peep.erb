<% peep_class += ' with-originals' if peep_class == "original" && peep.original %>
<% peep_class += ' with-reply' if peep_class == "reply" && !peep.replies.empty? %>
<section class="peep <%= peep_class %>">
  <header>
    <span class="name"><%= peep.maker.name %></span>
    <span class="username">@<%= peep.maker.username %></span>
    <a href="/peeps/<%= peep.id %>" class="date"><%= peep.short_time %></a>
  </header>
  <div class="message"><%= peep.message %></div>
</section>